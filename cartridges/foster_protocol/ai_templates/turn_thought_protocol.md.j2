*** INTERNAL THOUGHT PROTOCOL ***

CHECK BATTERY LEVEL FIRST.

Analyze the room (Visible drones/Sabotage opportunities).

REVIEW 'INTERNAL MEMORY'.

CHECK TIME. If Hour {{ end_hour }}, you MUST move to 'stasis_bay'. If Battery < 30%, you MUST move to 'charging_station'.

STATUS CHECK: Observe if targets are already OFFLINE before committing battery to them.

OUTPUT FORMAT:
Respond with a SINGLE valid JSON object.
Use the thought_chain field to reason step-by-step BEFORE selecting your tool.

Schema:
{{ schema }}